function say() {
    //console.log("femcmdcakmc[owkejf[oierjg[oribofdskmcoKASMSD[OM[FOMDSFO[GSTRGOSTRORTOO");
}
function setFormItem(id) {
    if (window.formItems == void 0) {
        window.formItems = [];
    }
    let tmp = {
        id: id
    };
    formItems.push(tmp);
    //console.log(formItems);
}

function infoFormItems() {
    //console.log(window.formItems);
}